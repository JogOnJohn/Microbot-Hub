package net.runelite.client.plugins.microbot.housetab.enums;

// Legacy route selector used by the script constructor. Most behavior is now
// controlled by HouseTabConfig, but this still documents the two entry styles.
public enum HOUSETABS_CONFIG {
    HOUSE_ADVERTISEMENT,
    FRIENDS_HOUSE,
}
